import styles from "./settings.module.css"

export default function ProjectSetting(){
  return(
    <div className={styles.setting}>
      <h1 className={styles.header}>プロジェクト設定</h1>
      <div className={styles.group}></div>
    </div>
  )
}